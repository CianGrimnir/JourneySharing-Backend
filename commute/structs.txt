

2 structs, one representing journey (source and destina)
struct_journey{
    source, 
    list destinations,
    current_loc,
    journey_id,
    list {users_id, drop_point}
}

struct_user{
    user_id,
    name,
    current_loc,
    radius,
    mode,
    destination, time
 }